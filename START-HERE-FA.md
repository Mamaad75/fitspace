# راه‌اندازی FitSpace 0.3.0

این بسته سورس کامل برنامه مستقل مدیریت باشگاه FitSpace است. فایل‌ها و کدهای پروژه جارچی در این بسته استفاده نشده‌اند.

## امکانات این نسخه

مدیریت چند باشگاه و شعبه، اعضا و مربیان، پلن و تمدید عضویت، کنترل اعتبار ورود، QR یک‌بارمصرف، برنامه تمرینی و غذایی، اندازه‌گیری و تصویر پیشرفت، موجودی و سفارش، ثبت پرداخت جزئی، رسید چاپی، اعلان و پیگیری عدم حضور، سطح دسترسی و پنل مدیر سامانه.

در نسخه ۰٫۲، اجرای مستقل روی Node، حساب ایمیل و رمز، تغییر و بازنشانی رمز، جست‌وجو و صفحه‌بندی سروری، گزارش بازه‌ای از همه رکوردها، CSV کامل اعضا و پرداخت‌ها، اجرای پنج‌دقیقه‌ای قواعد در حالت مستقل، پشتیبان‌گیری و بازیابی اضافه شده‌اند. قالب Docker و بررسی خودکار GitHub نیز در بسته وجود دارند.

## تازه‌های نسخه ۰٫۳

کلاس گروهی با شعبه، سالن، مربی، ساعت و ظرفیت؛ جلوگیری از تداخل ساعت سالن و مربی؛ رزرو عضو و ثبت رزرو توسط پذیرش؛ فهرست انتظار و جایگزینی خودکار؛ لغو کلاس و اعلان اعضا؛ حضورگیری مربی و خروجی فایل تقویم ICS اضافه شده‌اند.

راهنمای کامل این گردش کار و ارتقا در `RELEASE-0.3.0.md` است.

## اجرای محلی

پیش‌نیاز: Node.js نسخه 22.13 یا جدیدتر در شاخه 22، npm و ترمینال. برای دستورهای Bash از Linux، macOS یا WSL استفاده کنید.

در پوشه اصلی پروژه:

```bash
npm ci
npm run build:standalone
npm run account:create -- --email owner@example.com --name "مالک باشگاه" --owner
npm run start:standalone
```

به جای ایمیل نمونه، ایمیل مالک را وارد کنید. فرمان ساخت حساب رمز را پنهان دریافت می‌کند؛ رمز حداقل ۱۲ نویسه است. هیچ حساب یا رمز پیش‌فرضی در بسته نیست.

نشانی محلی: `http://localhost:3000`. با حساب ساخته‌شده وارد شوید، نام باشگاه را ثبت کنید، سپس پلن عضویت و اولین عضو را اضافه کنید. نگهداری اطلاعات و عکس‌ها در پوشه `data` انجام می‌شود. بستن برنامه اطلاعات را حذف نمی‌کند.

این دستور build خروجی `dist-standalone` می‌سازد. فرمان `npm run build` مخصوص خروجی Cloudflare است؛ برای سرور مستقل از `build:standalone` استفاده کنید.

## کاربران و نقش‌ها

از «حساب من» به `/account` بروید. مالک نصب از `/accounts` حساب می‌سازد؛ سپس مالک باشگاه ایمیل آن حساب را در «تیم باشگاه» با نقش مناسب ثبت می‌کند. برای نقش عضو، پرونده عضو مرتبط را انتخاب کنید. برای مربی، اعضای مربوط را به او اختصاص دهید. نقش عضو یا مربی به معنای دسترسی به همه باشگاه‌ها نیست.

کاربر در `/account` رمز خود را تغییر می‌دهد. تغییر رمز تمام نشست‌های قبلی همان حساب را باطل می‌کند. ارسال ایمیل دعوت و بازیابی رمز از طریق ایمیل در این نسخه متصل نیست؛ مدیر سرور می‌تواند رمز را بازنشانی کند:

```bash
npm run account:reset -- --email member@example.com
```

فرمان نیز رمز جدید را پنهان دریافت می‌کند. برای اجرا بدون ترمینال تعاملی، متغیر موقت `FITSPACE_PASSWORD` پشتیبانی می‌شود؛ آن را در Git یا فایل تنظیمات دائمی ذخیره نکنید.

مالک نصب مدیر پلتفرم است و می‌تواند پلن باشگاه‌ها را تعیین کند. تغییر پلن به معنی دریافت پول اشتراک نیست. باشگاه جدید ۱۴ روز امکانات حرفه‌ای دارد و پس از پایان دوره تابع محدودیت پلن پایه می‌شود.

## سرور واقعی و HTTPS

سرور مستقل باید یک نمونه Node داشته باشد و پشت reverse proxy دارای HTTPS اجرا شود. نمونه تنظیم محیط:

```bash
export NODE_ENV=production
export FITSPACE_ORIGIN=https://gym.example.com
export FITSPACE_DATA_DIR=/srv/fitspace-data
export PORT=3000
npm run start:standalone
```

`FITSPACE_ORIGIN` باید دقیقاً مبدأ عمومی مرورگر باشد؛ تطبیق مبدأ برای درخواست‌های تغییردهنده الزامی است. در حالت production، نشانی HTTP پذیرفته نمی‌شود. Reverse proxy درخواست‌ها را به پورت داخلی ۳۰۰۰ می‌فرستد. دامنه، DNS، گواهی HTTPS و فرایند دائمی سرویس را روی سرور خود پیکربندی کنید. API داخلی Node را با تنظیمات متفاوت از مبدأ عمومی منتشر نکنید.

فایل `.env` توسط اجرای مستقیم Node خودکار خوانده نمی‌شود؛ متغیرها را در shell یا مدیر سرویس تنظیم کنید. Docker Compose فایل `.env` را می‌خواند.

## Docker Compose

```bash
cp .env.example .env
```

در `.env` مقدار `FITSPACE_ORIGIN` را به دامنه HTTPS واقعی تغییر دهید. سپس:

```bash
docker compose build
docker compose run --rm app npm run account:create -- --email owner@example.com --name "مالک" --owner
docker compose up -d
```

Compose به شکل پیش‌فرض پورت را فقط روی `127.0.0.1:3000` میزبان باز می‌کند؛ reverse proxy میزبان به آن متصل می‌شود. داده‌ها در volume با نام منطقی `fitspace_data` ماندگار می‌شوند. `docker compose down -v` داده‌ها را حذف می‌کند؛ برای توقف معمولی از `docker compose stop` استفاده کنید.

قالب Docker در این محیط اجرا نشده است. ساخت Native Node و مسیرهای HTTP برنامه تست شده‌اند.

## گزارش و خروجی

در «گزارش‌ها» بازه حداکثر ۳۶۶روزه انتخاب کنید. فیلتر شعبه اعمال می‌شود. درآمد و تعداد ورودها از همه رکوردهای مجاز پایگاه داده محاسبه می‌شوند. CSV پرداخت‌ها تابع تاریخ است؛ CSV اعضا شامل همه اعضای شعبه انتخابی است. خروجی در حافظه مرورگر ساخته می‌شود و هنگام تغییر هم‌زمان رکوردها یک snapshot تراکنشی محسوب نمی‌شود.

فهرست اصلی ۵۰ رکورد در هر صفحه دارد و جست‌وجو در سرور انجام می‌شود. برخی بخش‌های جانبی پرونده، نمودار شخصی، کتابخانه حرکات و تاریخچه عملیات همچنان خلاصه‌ای از رکوردهای اخیر هستند؛ API صفحه‌بندی برای دسترسی برنامه‌ای به تمام رکوردهای مجاز موجود است.

## یادآوری خودکار

در حالت مستقل، تا زمانی که سرور روشن باشد، هر پنج دقیقه قواعد فعال برای باشگاه‌های دارای پلن مناسب اجرا می‌شوند. قواعد شامل انقضای عضویت، عدم حضور، موجودی کم و پایان برنامه هستند. اعلان‌ها داخل برنامه ثبت می‌شوند. پیامک، ایمیل یا push ارسال نمی‌شود. اجرای دستی در صفحه اتوماسیون هم وجود دارد. در نسخه Sites زمان‌بندی پس‌زمینه این بسته فعال نیست و اجرا دستی است.

## پشتیبان‌گیری و بازیابی

قبل از پشتیبان‌گیری، سرور و سایر فرایندهای نویسنده داده را متوقف کنید. بسته پشتیبان شامل پایگاه داده باشگاه، حساب‌ها و عکس‌هاست و باید خصوصی نگهداری شود.

```bash
# پس از توقف برنامه، از پوشه پروژه:
npm run backup -- backups/manual-01
```

در صورت استفاده از مسیر داده سفارشی، همان `FITSPACE_DATA_DIR` را تنظیم کنید. پوشه مقصد نباید از قبل وجود داشته باشد. فهرست فایل‌ها و SHA-256 در `manifest.json` ثبت می‌شود. این hash برای تشخیص خرابی تصادفی است و امضای دیجیتال در برابر مهاجم نیست.

بازیابی فقط به پوشه جدید انجام می‌شود:

```bash
FITSPACE_DATA_DIR=./data-restored npm run restore -- backups/manual-01
FITSPACE_DATA_DIR=./data-restored npm run start:standalone
```

بازیابی نشست‌های ورود قبلی را باطل می‌کند. صحت بازیابی پایگاه داده و فایل عکس در تست‌ها بررسی شده است. پشتیبان را به فضای جداگانه از دیسک اصلی نیز کپی کنید. اگر برنامه ناگهانی قطع شد و `server.pid` باقی ماند، قبل از حذف آن مطمئن شوید هیچ نمونه برنامه از همان پوشه استفاده نمی‌کند.

برای Docker: ابتدا `docker compose stop`، سپس با `docker compose run --rm -v "$PWD/backups:/backup" app npm run backup -- /backup/manual-01` پشتیبان بگیرید؛ پوشه `backups` میزبان باید برای کاربر container قابل نوشتن باشد. بازیابی را روی volume جدید انجام دهید.

## گیت‌هاب

این بسته شامل اسکریپت ایجاد یک مخزن جدید **خصوصی** است. هیچ توکن یا رمز گیت‌هابی در آن وجود ندارد. پس از نصب GitHub CLI و ورود به حساب خود:

```bash
gh auth login
bash scripts/publish-github.sh fitspace-gym-os
```

اسکریپت پروژه را commit می‌کند، یک مخزن خصوصی در حساب واردشده می‌سازد و فایل‌های کامل پروژه را push می‌کند. اگر نام مخزن از قبل وجود داشته باشد، متوقف می‌شود. می‌توانید نام دیگری به فرمان بدهید. در checkout دارای Git، تغییرات ابتدا باید commit شده باشند.

سورس در مخزن `https://github.com/Mamaad75/fitspace` ثبت شده است. نسخه ۰٫۲ در شاخه `release/0.2.0` و نسخه ۰٫۳ در `main` و `release/0.3.0` موجود است. اسکریپت بالا فقط برای انتشار یک کپی در مخزن تازه است؛ برای مخزن موجود دوباره آن را اجرا نکنید.

## تست و ساخت

```bash
npm test
```

این فرمان بررسی TypeScript، ساخت Worker، ساخت مستقل و تست‌ها را اجرا می‌کند. تست‌های دامنه روی SQLite و SQL migration واقعی اجرا می‌شوند؛ تست مستقل ورود HTTP، مسیر برنامه، کنترل مبدأ، اعتبار نشست و پشتیبان‌گیری را بررسی می‌کند. تست مرورگر، دستگاه واقعی و فشار عملیاتی انجام نشده است.

## مرزهای نسخه

این نسخه توسعه‌یافته و قابل اجراست؛ پرداخت آنلاین، بازپرداخت بانکی، درگاه پیامک/ایمیل، تمدید خودکار اشتراک مالی، کنترل ورود سخت‌افزاری و اپ بومی موبایل در آن پیاده نشده‌اند. ثبت دریافت وجه یک دفتر رسید است. شمارنده ظرفیت پلن تضمین کارایی برای یک میلیون عضو نیست؛ Node مستقل و SQLite برای یک نمونه سرور طراحی شده‌اند. برای عرضه وسیع، آزمون ظرفیت، مانیتورینگ و بررسی مرورگر/دستگاه لازم است.

فایل‌های SQL قبلی را تغییر ندهید؛ تغییر مدل باید migration جدید داشته باشد. نسخه مستقل تغییر hash migration اعمال‌شده را رد می‌کند.
