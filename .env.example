# Copy to .env for Docker Compose; use the real HTTPS origin without a trailing path.
FITSPACE_ORIGIN=https://gym.example.com
# Direct Node processes read shell environment variables; .env is not loaded automatically.
# FITSPACE_DATA_DIR=/absolute/path/to/data
# PORT=3000
