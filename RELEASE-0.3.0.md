# FitSpace ۰٫۳ — کلاس و رزرو گروهی

این نسخه بر پایه نسخه ۰٫۲ ساخته شده است. امکانات قبلی حفظ شده‌اند.

## استفاده از کلاس‌ها

۱. مالک، مدیر یا پذیرش از منوی «کلاس‌ها و رزرو»، یک کلاس با شعبه، نام سالن، زمان، مدت و ظرفیت می‌سازد. مربی اختیاری است. زمان فرم به وقت تهران است؛ نمایش تاریخ فارسی و ورودی تاریخ استاندارد مرورگر است.

۲. نام یکسان سالن در یک شعبه نباید بازه هم‌پوشان داشته باشد. یک مربی نیز نمی‌تواند دو کلاس هم‌زمان داشته باشد، حتی در شعبه‌های متفاوت. زمان پایان یک کلاس می‌تواند دقیقاً زمان شروع کلاس بعدی باشد.

۳. عضو کلاس‌های شعبه خودش را می‌بیند. برای رزرو، پروفایل او باید فعال و عضویت او در روز کلاس معتبر باشد. پذیرش نیز از پنل فهرست کلاس می‌تواند عضو همان شعبه را ثبت‌نام کند.

۴. اگر جای خالی وجود داشته باشد، رزرو تأیید می‌شود؛ در غیر این صورت عضو در فهرست انتظار قرار می‌گیرد. رزرو یا انتظار هم‌زمان برای کلاس‌های هم‌پوشان مجاز نیست. تکرار معمول درخواست، رزرو دوم ایجاد نمی‌کند.

۵. لغو رزرو تا قبل از شروع کلاس مجاز است. با آزادشدن یک رزرو قطعی، اولین فرد واجد شرایط در صف جایگزین می‌شود. اعضای غیرفعال، دارای عضویت نامعتبر یا منتقل‌شده به شعبه دیگر هنگام جایگزینی کنار گذاشته می‌شوند و در انتظار می‌مانند. اولویت بر اساس زمان ورود به صف و ترتیب درج در پایگاه داده است؛ رزرو دوباره پس از لغو، زمان صف جدید می‌گیرد.

۶. مالک، مدیر یا پذیرش می‌تواند کلاس آینده را لغو کند. تمام رزروها لغو می‌شوند و برای اعضا اعلان داخل برنامه ایجاد می‌شود. لغو یک کلاس، نفرات انتظار را به رزرو قطعی تبدیل نمی‌کند.

۷. مربی فقط فهرست کلاس‌های تعیین‌شده به خودش را می‌بیند. او یا مسئول مجاز باشگاه می‌تواند پس از شروع کلاس، حضور را ثبت کند. ثبت غیبت تنها پس از پایان کلاس مجاز است. این حضورگیری با ثبت ورود عمومی باشگاه و QR مستقل است.

۸. «فایل تقویم» یک فایل `.ics` می‌دهد که می‌توان آن را در تقویم شخصی وارد کرد. این فایل یک کپی از برنامه فعلی است، اشتراک تقویم زنده نیست و تغییرات بعدی کلاس را خودکار دریافت نمی‌کند.

## ارتقا از نسخه ۰٫۲ روی Node

- سرور را متوقف کنید و از داده‌ها با نسخه فعلی پشتیبان بگیرید.
- سورس ۰٫۳ را دریافت کنید؛ پوشه داده و متغیرهای محیطی موجود را حفظ کنید.
- `npm ci` و سپس `npm run build:standalone` را اجرا کنید.
- با همان `FITSPACE_DATA_DIR` قبلی، `npm run start:standalone` را اجرا کنید.
- migrationهای `0003` و `0004` به‌صورت خودکار اعمال می‌شوند. فایل‌های SQL قدیمی تغییر نکرده‌اند.
- یک کلاس آزمایشی آینده بسازید و ظرفیت/رزرو را بررسی کنید.

برای Docker، پس از پشتیبان‌گیری و توقف، image را دوباره بسازید و سرویس را با همان volume راه‌اندازی کنید. داده‌ها را داخل Git نگذارید.

بازگشت ایمن به نسخه ۰٫۲ یعنی توقف سرویس و بازیابی پشتیبان پیش از ارتقا در یک پوشه یا volume جدید، سپس اجرای سورس ۰٫۲ روی آن. downgrade خودکار schema ارائه نشده است.

## محدودیت‌های مشخص

کلاس تکرارشونده، پرداخت جداگانه هر کلاس، ویرایش زمان کلاس رزروشده، جریمه لغو، پیامک و همگام‌سازی زنده با Google Calendar در این نسخه وجود ندارند. برای تغییر زمان، کلاس آینده را لغو و یک کلاس جدید ایجاد کنید. اعلان‌ها داخلی هستند. فایل تقویم به‌تنهایی رزرو انجام نمی‌دهد.

کلاس‌ها برای همه پلن‌ها فعال‌اند و ظرفیت هر جلسه ۱ تا ۲۰۰ نفر است. سرور مستقل همچنان یک نمونه Node با SQLite است. از محدودیت ظرفیت جلسه نباید نتیجهٔ آزمون فشار یا ظرفیت کل سرویس گرفته شود.

## محل نسخه‌ها

- مخزن: https://github.com/Mamaad75/fitspace
- نسخه پیشین: `release/0.2.0`
- نسخه جدید: `main` و `release/0.3.0`

هر دو نسخه به‌صورت commit مستقل در تاریخچه نگه‌داری می‌شوند. به‌روزرسانی گیت‌هاب به معنی استقرار خودکار روی سایت زنده نیست.
